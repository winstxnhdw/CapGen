name: Clippy

on:
  pull_request:
  push:
    branches: [main]
    paths:
      - .github/workflows/clippy.yml
      - captions/**

jobs:
  clippy:
    uses: winstxnhdw/actions/.github/workflows/rust.yml@0832d473774c1447617374fce8cf68e75c50dd7f # main
    with:
      working-directory: captions
      disable-test: true
