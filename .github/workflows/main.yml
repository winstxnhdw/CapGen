name: Build

on:
  pull_request:
  push:
    paths:
      - .github/workflows/main.yml
      - .dockerignore
      - Dockerfile.build
      - pyproject.toml
      - uv.lock
      - transcriber/**
      - captions/**
      - server/**

jobs:
  test:
    strategy:
      matrix:
        working-directory: [server, transcriber, captions]
    uses: winstxnhdw/actions/.github/workflows/uv.yml@0832d473774c1447617374fce8cf68e75c50dd7f # main
    with:
      working-directory: ${{ matrix.working-directory }}

  build:
    uses: winstxnhdw/actions/.github/workflows/docker-build.yml@0832d473774c1447617374fce8cf68e75c50dd7f # main
    with:
      file: Dockerfile.build

  push:
    if: github.ref == 'refs/heads/main'
    needs: [test, build]
    uses: winstxnhdw/actions/.github/workflows/docker-push.yml@0832d473774c1447617374fce8cf68e75c50dd7f # main
    with:
      file: Dockerfile.build
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      packages: write

  restart:
    if: github.ref == 'refs/heads/main'
    needs: push
    uses: winstxnhdw/actions/.github/workflows/spaces-restart.yml@0832d473774c1447617374fce8cf68e75c50dd7f # main
    secrets:
      token: ${{ secrets.HF_TOKEN }}
